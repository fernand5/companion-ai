# Optional. Unset by default — the API client derives the backend URL from
# the page's own host (works for both localhost and LAN access). Set this
# only to point at an API on a different host/port.
# VITE_API_URL=http://localhost:8000/api
