# VITE_API_URL is optional — if unset, the API client derives it from
# whatever host the page was loaded from (see src/services/api.ts), so this
# works for both http://localhost:5173 and a LAN IP without editing this file.
# Set it explicitly only if the API is served from a different host/port.
