APP_ENV=testing

# Tests must never make real AI API calls (spec: mock the provider, no real
# network). Leaving this blank means GeminiProvider throws immediately unless
# a test explicitly binds a fake AiProvider or stubs Http::fake().
AI_API_KEY=
AI_PROVIDER=gemini
MEMORY_PROVIDER=database
AI_DEBUG=false
